# Python
__pycache__/
*.py[cod]
.venv/

# 本地配置与数据库(如需提交 .env 模板请用 .env.example)
.env
.env.*
!.env.example
*.db

# macOS
.DS_Store

# 日志 / 临时 / 编辑器
*.log
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# 上传的图片文件(元数据在数据库,文件本体不入库)
uploads/

# 测试产物
.pytest_cache/
.coverage
htmlcov/

# DeepSeek Harness 的工作目录(第七版):内含 node_modules(236 个包)、
# 按 viewer 生成的 profile patch,以及**会话 JSONL —— 那是完整的对话内容**。
# 一律不进 git。
.dsh/
